# Day 06 - Layers and MLPs

Day 06 composes neurons into networks.

## Goal

Compose neurons into layers, then layers into a small multilayer perceptron.

## Build

- `Layer`
- `MLP`
- composition over reusable modules

## Why It Matters

This is the first day the framework looks like a miniature model library.

## Exercise

Instantiate a tiny MLP and run one forward pass on toy data.
